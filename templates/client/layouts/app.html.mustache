<!doctype html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <script>
        var $rootId = "{{{ rootId }}}";
        var $reqId = "{{{ reqId }}}";
        var $state = {{{ state }}};
    </script>
    <script src="/js/app.js"></script>
</head>
<body>
    <div id="page">
        {{{ body }}}
    </div>
</body>
</html>